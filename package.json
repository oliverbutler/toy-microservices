{
  "scripts": {
    "build": "tsc",
    "start": "node ./bin/app.js",
    "dev": "ts-node ./src/app.ts",
    "deploy": "npm run build && cd terraform && terraform apply"
  },
  "dependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^4.7.3"
  },
  "devDependencies": {
    "ts-node": "^10.8.1"
  }
}
